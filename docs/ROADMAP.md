# ROADMAP

## Sprint 1
FIRE基本シミュレーター — 完了

## Sprint 2
取り崩し・追加投資ルール — 完了

## Sprint 3
暴落時の生活費調整・現金バッファ戦略 — 完了

## Sprint 4
AI FIREアドバイス（Gemini + ルールベースフォールバック） — 完了

## Sprint 5
NISA・iDeCo・年金の最適化 — 完了

- NISA年間投資枠・生涯非課税保有限度額の残りを計算
- NISA成長投資枠の残りを計算
- iDeCo年間拠出額を上限内で計算
- 年金受給開始年齢と年金見込額から受給開始後の年間不足額を計算
- 既存FIRE資産寿命シミュレーションは変更せず、最適化機能を分離

## Sprint 6
ユーザー保存・履歴管理 — 完了

- シミュレーション結果をJSON保存
- 最大20件の履歴管理
- 履歴一覧表示・個別削除・全削除
- 金融計算ロジックは変更せず保存機能のみ追加

## Sprint 7
UI改善・レポート出力 — 完了

- 保存済み履歴からレポート対象を選択
- HTMLレポートをダウンロード
- ブラウザの印刷機能でPDF化可能
- FIRE状態、入力条件、シナリオ、行動候補、NISA・iDeCo・年金関連を整理
- 金融商品の売買を断定しない注意事項をレポートへ明記
- 金融計算モジュールは変更せず、Sprint 6の履歴データを再利用

## Sprint 8
公開化・セキュリティガード — 完了

- 公開モード時の履歴をStreamlitセッション単位で分離
- セッションIDをハッシュ化して履歴ファイル名に利用
- セッション情報が取得できない場合の共有履歴ファイルへの書き込みを防止
- APIキーの存在確認は行うが秘密値そのものを画面へ表示しない
- 安全なエラー文言を提供
- 公開運用・セキュリティ確認ページを追加
- 金融計算ロジックは変更しない
- テスト32件すべて成功

## Sprint 9
シミュレーション結果の比較表示 — 完了

- 保存済み履歴から2〜4件を選択して主要指標を比較
- 先頭に選んだ履歴を基準に数値項目の差分を表示
- 資産寿命判定・純年間支出・推奨月間支出・現金生活費・目標現金・
  追加投資額・取り崩し額・今月の推奨行動・年金開始後の年間不足・
  NISA/iDeCo関連の指標を並べて表示
- Sprint 6の履歴データ（history_manager）を再利用し、金融計算ロジックは
  一切追加しない
- 付随修正として、app.pyの「全履歴を削除」ボタンで発生していた
  既存のNameErrorバグ（Path未import）を修正
- テスト41件すべて成功（既存32件 + Sprint 9 9件）

## Sprint 10
アプリケーションのイベント・エラーログ機能 — 完了

- シミュレーション実行、履歴の保存・削除、AIアドバイスのフォールバック理由などを
  ローカルのイベントログ（.fire_compass_events.log）へ記録
- 「📋 ログ・監視」ページでレベル別（INFO/WARNING/ERROR）に閲覧・全削除が可能
- 最大500件を超える古い記録は自動的に切り捨て
- 外部への送信は行わず、ログファイルはGit管理対象外
- Gemini APIエラー時にsafe_error_messageで安全な文言へ変換してから記録し、
  APIキー等の秘密値は記録しない
- 金融計算ロジック・AIアドバイスの内容は変更しない
- テスト54件すべて成功（既存41件 + Sprint 10 13件）

## Sprint 11
ログのCSVエクスポート — 完了

- 「📋 ログ・監視」ページに、現在表示中のレベルのログをCSVでダウンロードする
  ボタンを追加
- services/app_logger.pyへexport_events_to_csv() / events_export_filename()
  を追加（既存のlog_event / load_events / clear_eventsは変更しない）
- Excel（Windows）で文字化けしないよう、UTF-8 BOM付き・CRLF区切りで出力
- 金融計算ロジック・AIアドバイスの内容は変更しない
- テスト62件すべて成功（既存54件 + Sprint 11 8件）

## Sprint 12
シミュレーション比較結果のCSVエクスポート — 完了

- 「📊 シミュレーション比較」ページに、表示中の比較結果をCSVでダウンロード
  するボタンを追加
- services/comparison_engine.pyへexport_comparison_to_csv() /
  comparison_export_filename() / format_comparison_value()を追加
  （既存のbuild_comparison()は変更しない）
- Excel（Windows）で文字化けしないよう、UTF-8 BOM付き・CRLF区切りで出力
- 金融計算ロジック・AIアドバイスの内容は変更しない
- テスト70件すべて成功（既存62件 + Sprint 12 8件）

## Sprint 13
ログのキーワード検索・期間フィルタ — 完了

- 「📋 ログ・監視」ページにキーワード検索欄・開始日/終了日の日付入力を追加
- services/app_logger.pyへfilter_events()を追加
  （既存のlog_event / load_events / clear_events / export_events_to_csvは変更しない）
- レベル絞り込みと組み合わせて一覧表示・CSVエクスポートの両方に反映
- 金融計算ロジック・AIアドバイスの生成内容は変更しない
- テスト79件すべて成功（既存70件 + Sprint 13 9件）

## Sprint 14候補
- ドキュメントの表記統一（各docsファイルの見出し・記法の軽微な整理）
- JSONファイル保存方式（履歴・ログ）の運用限界の見直し
- 履歴（Sprint 6）の保存後の名称変更機能
